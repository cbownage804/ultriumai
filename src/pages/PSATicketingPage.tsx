import { PSATicketingSystem } from "@/components/psa/PSATicketingSystem";

const PSATicketingPage = () => {
  return (
    <div className="container mx-auto py-8">
      <PSATicketingSystem />
    </div>
  );
};

export default PSATicketingPage;